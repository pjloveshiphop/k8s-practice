apiVersion: v1
kind: Service
metadata:
  labels:
    app: zookeeper-service
  name: zookeeper-service
  namespace: kafka
spec:
  selector:
    app: zookeeper
  ports:
  - port: 2181
    name: zookeepr-port
    nodePort: 30181
    targetPort: 2181
  type: NodePort
